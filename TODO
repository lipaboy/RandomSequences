Global:

1) Распараллелить выполнение статистических тестов по последовательностям.

2) Не помешало бы написать "тесты на тесты", а то кто его знает

3) Реализовать ListOfVectors with iterator

Local:

1) Найти генератор, который проходит все тесты. Если генератор проходит все тесты, кроме LinearComplexity, то принять решение.

2) распараллелить умножение больших чисел в NIST библиотеке, чтобы ускорить работу BBS generator (но перед этим замерить время, занимаемое это умножение).
в теории получается, что это занимает 2^14 операций на одно такое умножение.


1) Read about RC-4 key-stream generator with
8-bit words [5]

- test it

2) Write tests for all stat tests

3) LibaboyLib - isn't a submodule of git. Connect it.

