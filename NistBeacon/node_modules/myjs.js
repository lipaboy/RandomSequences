var beacon = require('nist-beacon')
var epochTime = process.argv[2]

beacon.currentRecord(epochTime, function (err, res) {
  // prints the beacon value closest to epochTime in 128 hex characters
  //console.log('Next')
  console.log(res)
})
// beacon.nextRecord(epochTime, function (err, res) {
  // // prints the beacon value closest to epochTime in 128 hex characters
  // console.log('Next')
  // console.log(res)
// })
// beacon.previousRecord(epochTime, function (err, res) {
  // // prints the beacon value closest to epochTime in 128 hex characters
  // console.log('prev')
  // console.log(res)
// })
// beacon.lastRecord(function (err, res) {
  // // prints the last-published beacon value
  // console.log('last')
  // console.log(res)
// })